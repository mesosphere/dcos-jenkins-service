{
	"id": "jenkins",
	"instances": 1,
	"mem": 1000,
	"env": {
		"JAVA_OPTS": "-Xmx300m"
   },
   "uris": [
   	"http://mirrors.jenkins-ci.org/war-stable/latest/jenkins.war"
   ],
   "cmd": "git clone https://github.com/mesosphere/jenkins-on-mesos.git && cd jenkins-on-mesos && export JENKINS_HOME=$(pwd) && java -jar ../jenkins.war --httpPort=$PORT0"
}